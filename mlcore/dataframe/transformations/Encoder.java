package mlcore.dataframe.transformations;

public class Encoder {
    // Handles all kinds of feature encoding including but not limited to:
     /* 1. ordinal encoding
     * 2. one hot encoding
     * 3. label encoding
     */
    // The input passed to methods of this class must be a Dataframe (mlcore.dataframe.DataFrame)
    // The output returned from methods of this class must be a DataFrame (mlcore.dataframe.DataFrame)

}
