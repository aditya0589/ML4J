package mlcore.dataframe.transformations;

public class DataFrameTransformations {
    // Methods for some other DataFrame transformations

    public static DataFrame withColumn(String columnName, List<Object> values) {
        // Allows to add a new column to an existing dataframe
    }
    public static DataFrame withColumnReplaced(String OldColumn, String newColumn, List<Object> values) {
        // Allows to replace an old column with a new column
    }
}
